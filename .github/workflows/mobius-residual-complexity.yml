name: Mobius residual-complexity Lean verification

on:
  push:
    branches: [main]
    paths:
      - "MathematicalProofs/MobiusResidualComplexity/Lean/**"
      - ".github/workflows/mobius-residual-complexity.yml"
  pull_request:
    branches: [main]
    paths:
      - "MathematicalProofs/MobiusResidualComplexity/Lean/**"
      - ".github/workflows/mobius-residual-complexity.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Build the pinned Lean project
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: MathematicalProofs/MobiusResidualComplexity/Lean
          build: true
          build-args: "--wfail"
          test: false

      - name: Check the paper-wide certificate
        working-directory: MathematicalProofs/MobiusResidualComplexity/Lean
        run: lake env lean RiemannHypothesisProofFactory/MobiusResidualComplexity/PaperCertificate.lean
