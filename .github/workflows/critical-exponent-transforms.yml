name: Critical-exponent transforms Lean verification

on:
  push:
    branches: [main]
    paths:
      - "MathematicalProofs/CriticalExponentTransforms/Lean/**"
      - ".github/workflows/critical-exponent-transforms.yml"
  pull_request:
    branches: [main]
    paths:
      - "MathematicalProofs/CriticalExponentTransforms/Lean/**"
      - ".github/workflows/critical-exponent-transforms.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Build the pinned Lean project
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: MathematicalProofs/CriticalExponentTransforms/Lean
          build: true
          build-args: "--wfail"
          test: false

      - name: Check the paper-wide certificate
        working-directory: MathematicalProofs/CriticalExponentTransforms/Lean
        run: lake env lean RiemannHypothesisProofFactory/CriticalExponentTransforms/PaperCertificate.lean
